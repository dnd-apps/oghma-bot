type NickGroup {
    id: ID!
    name: String @search(by: [term])
    nickNames: [UserNick]!
}

type UserNick {
    user: String! @id @search(by: [hash, regexp])
    nick: String! @search(by: [hash, regexp])
}

